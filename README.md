# dracox

> [!CAUTION] AI Slop Alert
> The initial commit was mostly generated mostly by Claude 4.5 because this is a D-list feature that I was hoping someone else would handle.

This library uses the absolute minimum of `draco` so `trimesh` can support the `KHR_ext_draco` extension in compressed GLTF files. It only supports `decode`.
